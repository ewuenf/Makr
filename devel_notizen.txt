Fragen/Probleme:
***************

Was passiert, wenn ein Unterscript beendet wird mit exit?
  SystemExit-exception, Multithreading scheint ein Problem zu sein


TODO:
*****

Beispiele aufsplitten oder alle Möglichkeiten in einem großem Beispiel-Makrfile (oder beides ;-) )
Unterprojekte möglichst abgetrennt
Named arguments umsetzen?

Datei zu ArrayOfTasks-Hash? ( convenient, z.B. zum umfassenden Build *einer* Datei über eine IDE wie eclipse mit allen relevanten Tasks

                              Alternative ist über alle Tasks iterieren und über ein generelles fileName-Attribut 
                              einsammeln was schwierig ist bei Tasks die mit mehreren Dateien zu tun haben wollen 
                              (also eher fileNames-Array pro task?)
                            )

Update bei Dateien: Wenn md5 verwendet, dann nur md5 entscheidend, ansonsten nur Dateiattribute (mod-time || size)